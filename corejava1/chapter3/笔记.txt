Java 是一种强类型语言
共有8种基本类型
4种整数int short long byte
2种浮点数float double
1种字符类型char
1种表示真值的boolean

所有的NaN的值都认为是不同的
使用if (double.isNaN(x))
判断

3.3.3
char 类型的字面量值需要使用单引号括起来
char 可以表示为16进制值范围为
\u0000 ~ \u9999

可以在字符常量和字符串中使用
还可以子其外使用(其他转义序列不可以)
public static void main(String\u005B\u005D args)
和
public static void main(Stringp[] args)相同
Unicode 的转义字符会在解析代码之前处理
小心注释中的\u
// \u000A 会产生一个语法错误
因为\u000A会替换为一个换行符
// 如路径中c:\users
也是语法错误因为\u后面没有跟着4位16进制数
那么如何输入\u 使用转义字符\\u
或者使用\的unicode的表示\u005C
\u005Cu即为\u,转义字符的解析只会解析一次

码点 只字符对应的代码值
代码平面有17个

对于变量，无法使用无法使用未初始化的变量

Math.pow(x, a)返回x的a次幂

Math类，的如果一个计算溢出,数学运算符只是返回错误结果
而没有任何提醒
允许使用+连接两个字符串

3.6.1 子串

String geeting = "Hello";
greeting.substring(0, 3);
返回一个"Hel" 
可以使用+连接连接有一个字符串和非字符串
后者会被转化为字符串
String.join(" / ", "S", "M", "L")
可以将多个字符串连接，中间使用 / 分割
字符串重复
String repeated = "Java“.repeat(3);
repeated ==> "JavaJavaJava"

检测字符串是否想等equals

3.6.9

字符串构建器
StringBuilder builder = new StringBuilder();

builder.append(ch); // 增加单个字符
builder.append(str);  // 一个字符串

String completedString = builder.toString();
得到String1
类似的类StringBuffer 效率不如StringBuilde
但是前者采用多线程操作字符串

3.8.5 多重选择switch 四种

3.8.6 中断控制流程的语句

break
和带标签的break
标签，必须放在你想跳出的最外层循环之前，并且紧跟一个冒号
可以跳出语句块，而不能跳入
也有continue